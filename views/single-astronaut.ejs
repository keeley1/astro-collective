<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>astronaut</title>
    <link rel="stylesheet"  type="text/css" href="/main.css" />
</head>
<body>
    <% if (appState === 'loggedin') { %>
        <p><a href="/logout" id="sign-up">log out</a></p>
    <% } else { %>
        <p><a href="/login" id="sign-up">log in</a></p>
    <% } %>
    
    <div class="navbar">
        <div class="navbar-container">
            <div class="navbar-item-one">
                <h1><a href="/" id="app-title"><%=appName%></a></h1>
            </div>
            <div class="navbar-item-two">
                <p><a href="/astronauts" class="navbar-astro <%= currentPage === 'astronauts' ? 'active' : '' %>">astronauts</a></p>
            </div>
            <div class="navbar-item-two">
                <p><a href="/missions" class="navbar-astro">missions</a></p>
            </div>
            <div class="navbar-item-two">
                <p><a href="spacecraft" class="navbar-astro">spacecraft</a></p>
            </div>
            <div class="navbar-item-two">
                <p><a href="about" class="navbar-astro">about</a></p>
            </div>
        </div>
    </div>

    <p><a href="/astronauts" class="single-data-back"><</a></p>
    
    <% if (astronaut.length > 0) { %>
        <div class="single-data-container">
            <% astronaut.forEach(function(astronaut) { %>
                <div class="single-data">
                    <div class="single-data-flex-container">
                        <div class="single-data-flex-one">
                            <img src="<%= astronaut.astronaut_photo %>" alt="<%= astronaut.astronaut_name %>" class="single-data-photo" width="300">
                        </div>
                        <div class="single-data-flex-two">
                            <h3 class="single-data-title"><%= astronaut.astronaut_name %></h3>

                            <% if (astronaut.date_of_death === 'N/A' || astronaut.date_of_death === null) { %>
                                <p class="single-data-data"><%= astronaut.date_of_birth %></p>
                            <% } else { %>
                                <p class="single-data-data"><%= astronaut.date_of_birth %> - <%= astronaut.date_of_death %></p>
                            <% } %>

                            <p class="single-data-data">Country: <%= astronaut.country %></p>
                            <p class="single-data-data">Hours in Space: <%= astronaut.hours_in_space %></p>
                        </div>
                    </div>
                    <p class="single-data-data" id="single-data-profile">Astronaut Profile: <%- astronaut.astronaut_profile.replace(/\r\n/g, '<br>') %></p>    
                </div>
             <% }); %>
        </div>
    <% } else { %>
        <p>No astronauts available.</p>
    <% } %>

</body>
</html>